const { open } = require('node.fs/promises');
var quotes = [];


function readQuotes() {
  
}

// The method that is going to print the qoute to the console
function printQuote(quote) {
   console.log(quote);
}

printQuote('js is still working')


